package com.github.nahnullscience.cypher_nexus.mechanic.wand.module.component

/**
 * marks a module not slot-unique, multiple instances of that type can exist at the same time
 * */
sealed interface ITypeStackableModule